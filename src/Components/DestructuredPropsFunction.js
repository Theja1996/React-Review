import React from 'react'

const Depro = (props)=>{
    const {name, hero}= props
    return(

        <div>
        <h1> One of the Awesome movie {name} love with {hero}</h1>
        </div>
    )
  
}

export default Depro;